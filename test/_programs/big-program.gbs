program {
	Sarasa()
	a := 3

    return(a * 2)
}

procedure Sarasa(){
    Poner(Rojo)
}

function hayCaramelo () {
 return (hayBolitas(Azul) || hayBolitas(Verde) || hayBolitas(Rojo) || hayBolitas(Negro))
}
